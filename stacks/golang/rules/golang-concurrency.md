# Golang Concurrency Rules

## 目标

规范 Go 项目中的并发实现，减少 goroutine 泄漏、锁竞争和取消路径缺失。

## 要求

1. 新增 goroutine 必须有明确退出条件，禁止无管理后台循环。
2. channel 的关闭方和消费方职责必须清晰，避免重复关闭或永不关闭。
3. 并发访问共享状态时必须说明使用锁、原子操作还是串行化策略。
4. 涉及 context 时必须沿调用链向下传递，不能在中途丢失取消信号。
5. 发生超时、取消或部分失败时，必须说明资源释放和补偿逻辑。
