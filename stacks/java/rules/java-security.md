# Java Security Rules

## 目标

补充 Java / Spring 服务的实现级安全红线，降低输入处理、认证授权和文件操作带来的风险。

## 规则

1. 外部输入进入 SQL、路径、命令、模板或脚本执行前必须先做参数化、白名单或类型校验。
2. 文件上传必须校验类型、大小、目标路径和存储位置，禁止直接信任原始文件名或扩展名。
3. 密码、令牌、密钥和敏感个人数据不得明文落日志、响应体或调试输出。
4. 涉及认证、授权、租户隔离的改动，必须明确访问控制不会被放宽、绕过或旁路。
5. 引入安全相关依赖、加密算法或反序列化能力时，必须说明默认配置和风险边界。
