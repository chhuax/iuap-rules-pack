# Java Database Change Rules

## 目标

规范 Java 项目中的数据库变更说明，降低表结构调整和 SQL 改动带来的联动风险。

## 要求

1. 表结构、索引、约束调整必须说明兼容窗口和回退方式。
2. 变更脚本应保证幂等或说明重复执行后的处理方式。
3. 涉及历史数据回填、批量修复或默认值修正时，必须说明执行批次和耗时风险。
4. ORM 映射、SQL 语句和数据库脚本要保持一致，避免只改其中一层。
5. 线上大表变更必须标记锁表风险、执行窗口和观察指标。
