# Java Code Review Rules

## 目标

统一 Java 代码评审时的关注点，优先发现分层失真、异常处理和可维护性回归。

## 评审重点

1. Controller 只负责协议转换和参数校验，不承载核心业务编排。
2. Service 层要保证事务边界清晰，避免在链路中隐式开启或扩大事务。
3. Repository / DAO 层查询要关注索引命中、分页约束和批量操作边界。
4. 异常处理不能只打印日志或直接吞掉，返回语义必须与调用方约定一致。
5. 日志、监控、审计字段要与业务动作对应，避免只有失败日志没有关键成功路径记录。
