# IUAP Release Readiness Rules

## 目标

统一上线前最小检查项，减少因为配置、回滚、兼容性遗漏导致的发布事故。

## 发布前要求

1. 必须说明本次发布的影响范围、依赖服务和回滚触发条件。
2. 配置变更要区分默认值、环境差异和灰度策略，禁止只在本地验证。
3. 接口、数据库、消息体变更必须说明兼容性与回退顺序。
4. 高风险改动应明确监控指标、告警阈值和观察窗口。
5. 未覆盖测试、未完成联调或待确认事项必须显式列出，不能在结论中省略。
