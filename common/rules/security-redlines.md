# IUAP Security Redlines

## 目标

定义企业项目中的最小安全红线，避免在交付阶段忽略敏感数据、权限与审计问题。

## 红线

1. 不允许把密钥、令牌、密码、连接串直接写入源码、脚本或示例配置。
2. 涉及权限、身份、租户隔离的改动，必须说明访问控制是否被放大或绕过。
3. 不允许用关闭鉴权、放宽白名单、跳过审计的方式临时让流程通过。
4. 输出日志时必须避免打印身份证号、手机号、银行卡号、token 等敏感字段。
5. 涉及外部接口、文件上传、动态脚本执行时，必须说明输入校验和失败处理策略。
